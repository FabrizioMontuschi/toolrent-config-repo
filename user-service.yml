spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: user_user
    password: user_pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 0
